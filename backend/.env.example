# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Database (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/luxurio?schema=public"

# Auth
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Default admin (used by seed)
ADMIN_EMAIL=admin@luxurio.local
ADMIN_PASSWORD=admin123
